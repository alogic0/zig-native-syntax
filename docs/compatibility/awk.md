# AWK highlighting

The verified structural AWK backend distinguishes regular-expression literals
from division using expression context. It recognizes pattern/action keywords,
user functions and parameters, calls, fields, variables, builtin functions and
variables, GNU AWK directives, strings, escapes, numbers, and comments. It is a
tolerant highlighter and does not validate dialect-specific runtime behavior.
