# Nushell highlighting

The verified structural Nushell backend tracks pipeline and command positions,
multiword builtin command families, custom and exported declarations,
signatures and closure parameters, flags, variables and cell paths, record
keys, modules, and external commands. It preserves malformed input without
executing commands or attempting Nushell's runtime type inference.
