# C# highlighting

The verified C# backend layers the shared tolerant declaration parser over its
lexical scanner. It distinguishes namespaces, declared types, constructors,
methods, parameters, variables, calls, and member properties while preserving
incomplete source and preprocessor lines.
