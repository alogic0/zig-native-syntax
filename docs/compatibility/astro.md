# Astro highlighting

The verified Astro backend composes component markup with the JavaScript
parser for frontmatter, script bodies, and brace expressions. Markup comments,
tags, attributes, strings, and malformed outer boundaries remain owned by the
source-preserving component scanner. Style bodies remain embedded text.
