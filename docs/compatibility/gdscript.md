# GDScript highlighting

The verified structural GDScript backend recognizes class and base types,
functions, signals, parameters, typed variables, enums and members,
annotations, node paths, calls, and properties. It supports ordinary and
triple-quoted strings and recovers at line boundaries for malformed ordinary
strings. It does not resolve inferred types, scene nodes, or engine APIs.
