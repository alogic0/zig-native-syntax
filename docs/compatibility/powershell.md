# PowerShell highlighting

The verified PowerShell backend combines case-insensitive lexical scanning with
a tolerant declaration pass for functions, filters, workflows, classes, and
enums. Variables, type annotations, cmdlet calls, comments, strings, escapes,
values, and operators remain source preserving. It does not execute expansion
or resolve commands.
