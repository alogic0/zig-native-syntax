# Java highlighting

The verified Java backend combines its lexical scanner with the shared
tolerant declaration parser for packages, classes, records, interfaces,
methods, constructors, parameters, variables, calls, and member properties.
