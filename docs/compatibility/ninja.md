# Ninja Highlighting Compatibility

The focused `ninja` scanner is verified for lexical highlighting. It recognizes
declarations, rule and pool labels, build outputs, rule names, inputs, bindings,
variables, dollar escapes, quoted command fragments, continuations, and
comments. It does not evaluate the build graph or parse command shell dialects.
