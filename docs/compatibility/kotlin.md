# Kotlin highlighting

The verified Kotlin backend uses the tolerant declaration parser for packages,
classes and objects, functions, name-before-type parameters, properties,
variables, calls, and member selectors while retaining bounded lexical
recovery.
