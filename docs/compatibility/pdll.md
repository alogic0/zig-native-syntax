# PDLL highlighting

The verified PDLL backend parses pattern, rewrite, and constraint declaration
heads, parameters, `let` bindings, builtin constraint types, calls, and member
access. Its lexical layer preserves comments, strings, escapes, operations,
values, and incomplete source without evaluating patterns.
