# LLVM IR highlighting

The verified LLVM IR scanner recognizes SSA values, global symbols and
functions, metadata references, block labels, instructions, types, constants,
strings, byte escapes, comments, operators, and punctuation. It preserves
unknown target and intrinsic syntax without validating the IR module.
