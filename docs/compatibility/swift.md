# Swift highlighting

The verified Swift backend layers tolerant declaration parsing over lexical
highlighting for nominal types, functions, name-before-type parameters,
bindings, calls, member selectors, attributes, comments, strings, and values.
It preserves incomplete source without resolving overloads or extensions.
