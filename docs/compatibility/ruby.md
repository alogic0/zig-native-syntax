# Ruby highlighting

The verified Ruby backend combines lexical scanning with a tolerant pass for
modules, classes, methods, and parenthesized parameters. Instance and class
variables use variable scope, while calls, comments, strings, escapes, values,
and operators remain source preserving.
