# Go highlighting

The verified Go backend combines bounded lexical scanning with the shared
tolerant declaration parser. It distinguishes packages, declared types,
functions, parameters, variables, calls, and selector properties while
preserving incomplete source. It does not type-check or resolve imports.
