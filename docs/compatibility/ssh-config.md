# SSH Config Highlighting Compatibility

The focused `ssh-config` scanner is verified for lexical highlighting. It
recognizes Host and Match sections, option names, assignment operators, quoted
strings and escapes, percent tokens, ports, booleans, and comments. It is
line-bounded and does not validate option-specific value grammars.
