# KDL Highlighting Compatibility

The focused `kdl` scanner is verified for lexical highlighting. It recognizes
node names, properties, primitives, quoted and raw strings, escapes, comments,
slashdash markers, operators, and structural punctuation with bounded malformed
recovery. It does not validate the KDL document grammar or type annotations.
