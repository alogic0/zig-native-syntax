# Windows Batch highlighting

The verified `batch` scanner recognizes case-insensitive commands, `REM` and
`::` comments, labels, percent and delayed variables, positional arguments,
caret escapes, strings, numbers, redirection, command separators, and
punctuation. The `bat` and `cmd` labels route to this backend.
