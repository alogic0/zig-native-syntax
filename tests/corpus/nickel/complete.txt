let make_item = fun name enabled => {
  name | String | doc "display name" = name,
  enabled = enabled,
  nested.count = 42,
  message = "hello %{name}\\n",
} in
let item = make_item "demo" true in
item.name
# comment
