use fmt;
use io;

type coords = struct {
    x: int,
    y: int,
};

def DEFAULT_LIMIT: size = 5;
let global_count: int = 0;

fn translate(point: coords, dx: int) coords = {
    return coords {
        x = point.x + dx,
        y = point.y,
    };
};

export fn main() void = {
    const origin = coords { x = 1, y = 2 };
    const moved = translate(origin, DEFAULT_LIMIT);
    let enabled: bool = true;
    fmt::printfln("x={}\\n", moved.x)!;
};
// comment
